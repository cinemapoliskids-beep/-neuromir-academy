const fs = require('fs');
function getDimensions(filePath) {
    // We can just use the standard sizeOf or read the first few bytes. 
    // Wait, node doesn't have a built-in image size module.
    return "need module";
}
